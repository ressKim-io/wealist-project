name: CI - Dev User Service

on:
  push:
    branches:
      - deploy-dev
    paths:
      - "user-service/**"
      - ".github/workflows/ci-dev-user-service.yml"
  workflow_dispatch:

# env section removed - values loaded from Parameter Store in steps

permissions:
  contents: read

jobs:
  build-and-push:
    name: Build & Push to ECR
    runs-on: ubuntu-latest
    environment: development

    outputs:
      image-tag: ${{ steps.image-info.outputs.image-tag }}
      full-image: ${{ steps.image-info.outputs.full-image }}

    steps:
      # ============================================
      # 1. 코드 체크아웃
      # ============================================
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      # ============================================
      # 2. Java 환경 설정 (JDK 21)
      # ============================================
      - name: ☕ Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "21"
          cache: "gradle"

      # ============================================
      # 3. Gradle 빌드 & 테스트
      # ============================================
      - name: 🔨 Build with Gradle & Run Tests
        run: |
          cd user-service
          chmod +x gradlew
          ./gradlew clean build

      # ============================================
      # 4. 테스트 커버리지 리포트 업로드
      # ============================================
      - name: 📊 Upload JaCoCo Coverage Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jacoco-report-user-service-${{ github.sha }}
          path: user-service/build/reports/jacocoHtml
          retention-days: 7

      # ============================================
      # 5. AWS Credentials 설정
      # ============================================
      - name: 🔐 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.WEALIST_DEV_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.WEALIST_DEV_AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      # ============================================
      # 6. Parameter Store에서 설정 로드
      # ============================================
      - name: 📥 Load Configuration from Parameter Store
        id: config
        run: |
          echo "📥 Loading CI configuration from Parameter Store..."
          
          AWS_REGION=$(aws ssm get-parameter \
            --name "/wealist/dev/ci/aws_region" \
            --query 'Parameter.Value' \
            --output text)
          
          ECR_REPOSITORY=$(aws ssm get-parameter \
            --name "/wealist/dev/ci/ecr_repository_user" \
            --query 'Parameter.Value' \
            --output text)
          
          AWS_ACCOUNT_ID=$(aws ssm get-parameter \
            --name "/wealist/dev/ci/aws_account_id" \
            --query 'Parameter.Value' \
            --output text)
          
          # Validate parameters
          if [ -z "$AWS_REGION" ] || [ -z "$ECR_REPOSITORY" ] || [ -z "$AWS_ACCOUNT_ID" ]; then
            echo "❌ Failed to load required parameters from Parameter Store"
            echo "   Please ensure the following parameters exist:"
            echo "   - /wealist/dev/ci/aws_region"
            echo "   - /wealist/dev/ci/ecr_repository_user"
            echo "   - /wealist/dev/ci/aws_account_id"
            exit 1
          fi
          
          # Export to GITHUB_ENV for subsequent steps
          echo "AWS_REGION=${AWS_REGION}" >> $GITHUB_ENV
          echo "ECR_REPOSITORY=${ECR_REPOSITORY}" >> $GITHUB_ENV
          echo "AWS_ACCOUNT_ID=${AWS_ACCOUNT_ID}" >> $GITHUB_ENV
          
          # Also export to outputs
          echo "aws-region=${AWS_REGION}" >> $GITHUB_OUTPUT
          echo "ecr-repository=${ECR_REPOSITORY}" >> $GITHUB_OUTPUT
          echo "aws-account-id=${AWS_ACCOUNT_ID}" >> $GITHUB_OUTPUT
          
          echo "✅ Configuration loaded successfully"
          echo "  - AWS Region: ${AWS_REGION}"
          echo "  - ECR Repository: ${ECR_REPOSITORY}"
          echo "  - AWS Account ID: ${AWS_ACCOUNT_ID}"

      # ============================================
      # 7. 이미지 태그 생성
      # ============================================
      - name: 🏷️ Generate Image Tags
        id: image-info
        run: |
          ECR_REGISTRY="${{ steps.config.outputs.aws-account-id }}.dkr.ecr.${{ steps.config.outputs.aws-region }}.amazonaws.com"
          IMAGE_TAG="${GITHUB_SHA:0:7}"
          FULL_IMAGE="${ECR_REGISTRY}/${{ steps.config.outputs.ecr-repository }}:${IMAGE_TAG}"
          LATEST_IMAGE="${ECR_REGISTRY}/${{ steps.config.outputs.ecr-repository }}:latest"

          echo "ecr-registry=${ECR_REGISTRY}" >> $GITHUB_OUTPUT
          echo "image-tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "full-image=${FULL_IMAGE}" >> $GITHUB_OUTPUT
          echo "latest-image=${LATEST_IMAGE}" >> $GITHUB_OUTPUT

          echo "📦 Image Tag: ${IMAGE_TAG}"
          echo "🐳 Full Image: ${FULL_IMAGE}"

      # ============================================
      # 8. Amazon ECR 로그인 & 이미지 빌드 & 푸시
      # ============================================
      - name: 🐳 Login to ECR, Build and Push Docker Image
        env:
          FULL_IMAGE: ${{ steps.image-info.outputs.full-image }}
          LATEST_IMAGE: ${{ steps.image-info.outputs.latest-image }}
          ECR_REGISTRY: ${{ steps.image-info.outputs.ecr-registry }}
        run: |
          # ECR 로그인
          echo "🔑 Logging into Amazon ECR..."
          aws ecr get-login-password --region ${{ steps.config.outputs.aws-region }} | \
            docker login --username AWS --password-stdin ${ECR_REGISTRY}

          # Docker 이미지 빌드
          cd user-service
          echo "🔨 Building Docker image..."
          docker build \
            --build-arg CACHEBUST=$(date +%s) \
            -t ${FULL_IMAGE} \
            -t ${LATEST_IMAGE} \
            .

          # ECR에 푸시
          echo "📤 Pushing to ECR..."
          docker push ${FULL_IMAGE}
          docker push ${LATEST_IMAGE}

          echo "✅ Successfully pushed:"
          echo "  - ${FULL_IMAGE}"
          echo "  - ${LATEST_IMAGE}"

      # ============================================
      # 9. 빌드 결과 요약
      # ============================================
      - name: 📋 Build Summary
        run: |
          echo "### ✅ User Service Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ steps.image-info.outputs.full-image }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** \`${{ steps.image-info.outputs.image-tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** \`${{ github.workflow }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Run ID:** \`${{ github.run_id }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps:**" >> $GITHUB_STEP_SUMMARY
          echo "- CD workflow should be triggered automatically via \`workflow_run\`" >> $GITHUB_STEP_SUMMARY
          echo "- Check [Actions tab](${{ github.server_url }}/${{ github.repository }}/actions) for CD workflow" >> $GITHUB_STEP_SUMMARY
          echo "- If CD doesn't trigger, manually run it from [CD workflow page](${{ github.server_url }}/${{ github.repository }}/actions/workflows/cd-dev-user-service.yml)" >> $GITHUB_STEP_SUMMARY

